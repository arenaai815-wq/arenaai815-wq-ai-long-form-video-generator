# Next.js server-side configuration (frontend/.env.local for local overrides).
# The browser never receives backend URLs or secrets: /api/* is proxied by next.config.mjs.

# Where the Next.js server reaches the FastAPI backend (server-to-server).
API_INTERNAL_URL=http://localhost:8000

